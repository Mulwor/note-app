Что такое middleware & depends, в чем из отличие в применении

1. Зачем в FastAPI конструкция Depends()
2. В чем отличие depends от dependencies

dependencies - это проверка и оно ничего не возвращает. А depends() возвращает

3. Как добавить ограничение запросов на ручку?

Можно исп библиотеку slowapi, где есть Limiter и мы можем туда записать например 3 запроса в минуту и как декоратор использовать

4. Какой встроенный механизм выполнения фон.задач можно встроить в Fast api и как он работает на самом деле

BackgroundTasks

5. Плюсы и минусы в сравении с Django

- Высокая производительность
- Автодокументируемый
- Типизация через Pydantic
- И он легкий вплане веса

6. На каком фреймворке построен FastAPI?

7. Как настроить CORS в FastAPI?

берем app и добавляем миддлвары, где первым аргументом пишем CORSMiddleware, а дальше указываем те заголовоки которым можно обращаться, запросы и прочее

8. Как написать кастомный middleware через Fast Api

Нужно написать class который будет наследоваться от Base HTTP Middleware, переопределить метод диспатч, вызвать метод callnext который отработает до обработки запроса и после обработки что-то изменить в заголовке и вернуть респонс

9. Что такое Pydantic схема и зачем она нужна

10. Что такое response_model в Pydantic и зачем их исп

Они нужны для того, чтобы отправлять фронту нужные данные - например из пайтона преобразовать в json

11. Как сервер исп для развертывания приложения FastAPI?

Uvicorn

12. Что исп для тестирования в FastAPI?

TestClient для синхронных тестов
Pytest
Pytes-asyncio

13. Как в fastAPI замокировать зависимость
14. Что такое lazy_load / eager_load

lazy_load запрос загружается по отдельности, только когда они необходимы

eager_load запрос загружает все сразу при первом обращении, и в последующем запросы не будут загружаться

15. Разница между session и sessionmaker

Sessionmaker это фабрика где создаются сессии, а сессия это просто запросы которые мы выполняем к нашей базе данных
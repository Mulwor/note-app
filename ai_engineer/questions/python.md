1. Что такое словарь (dictionary)? Что может быть ключом в словаре

Словарь в питоне реализован через хеш-таблицы. Хеш-таблица это такая структура памяти когда мы выделяем определенное пространство для каких-то записей в нашей памяти, соответственно когда мы обращаемся к какому-то ключу мы берем хеш этого ключа, затем берем остаток от деления хеша на кол-во ячеек, который мы выделили в памяти и соответственно получаем номер ячейки куда мы и записываем наш результат

И чтобы хеш от ключей не менялся ключи должны быть неизменяемыми типами данных

В качестве ключа может быть любой неизменяй тип данных 

2. Какие методы должны быть определены у типа, чтобы их можно было использовать как ключи

3. Что такое итератор и генератор

Итератор - это объект который имеет два метода это __iter__ и __next__. Метод __iter__ должен возвращать сам объект итератора, а метод __next__ должен возвращать следующее по счету значения 

Генератор особый вид итератора который не хранит все вычисляемую последовательность в виде списка, а вычисляет каждое след.значения при помощи какой-то формулы

4. Какие есть методы у генератора, но нет у итератора:

метод send() - который автоматически отправляет значения внутрь генератора
метод throw() - отправляет ошибку, что полезно для отладки
метод close() - завершает итератор и выводит Generator exit

6. Что такое *args и **kwargs? Можно ли написать *lol, **kek

args - неименованые аргументы, kwargs - именованые аргументы. Так в питоне можно очень удобно передать в качестве значений функций, массив или словарик

Аргс попадают в виде кортежа внутри функции, а кваргс в виде словарей

Именованный и неименованные аргументы и при помощи них можно делать функции которые принимают бесконечное число аргументов

7. Что такое lambda-функции

8. Что такое контекстный менеджер

Контекстный менеджер - объект класса, который исп вместе с итератором with. Если мы хотим, чтобы объект стал контекстным менеджером мы должны написать два метода enter (при входе в контекст), а exit при выходе

Можно ли сделать контекст.менеджер асинхронным? - Да

9. Асинхронный код Python. Основные преимущества асинхронности и как работать с асинхронностью
10. Чем отличаются многопоточное и многопроцессное приложение?
11. Как реализуется многопоточность в Python?
12. Что такое GIL? Как он работает? Какие проблемы возникают из-за его использования?

GIL - это global interpreter lock - основная задача в том, чтобы не давать интерпретатору питона работать больше одного треда (потока) за раз 

Как обойти его? 

- В 3.11 - Субинтерпретатор внесли когда можно запустить несколько изолированных сред со своим ГИЛ
- В 3.13. - Завесли тестовую версию полностью откл гила

Каждую задачу можно разделить на подзадачи

11. Что хранится в атрибуте dict 
12. Что такое слайс(slice)
13. Как проверить, что один кортеж содержит все элементы другого кортежа
14. Для чего используется ключевое слово yield

15. Что такое ООП
16. Какие основные принципы объектно-ориентированного программирования (ООП) ты знаешь

16. Что такое __slots__
17. Как работать с асинхронностью
18. Почему асинхронное программирование быстрее чем реализация на потоках. 

Так как при асинхронном программировании мы не тратим время на переходы между потоками и по этому ты тратим меньше времени

19. Какие методы нужно реализовать чтобы наш класс был ключом в словаре?

Метод __hash__ для получения хеша по данному ключу и __eq__ нужен для сравнения для избежаний коллизий 

20. Какой результат и почему
```js
d1 = {1 : 1, 2 : 2, 3 : 3}
d2 = d1
d2[1] = 0
print(d1)
print(d2)
```

В обоих случаях будет 0, так как происходит присваивания 

21. Какой результат и почему

def f1(my_list)
  my_list = [4, 5, 6]

l1 = [1, 2, 3]
l2 = [1, 2, 3]
f1(l1)
print(l1)

f2(l2)
print(f2)

22. Какой результат и почему

```.py
l = [1,2,3]
def f(a, L = [])
  L.append(a)
  return L
f(4)
f(5)
tmp_l = f6
print(tmp_l)

f(7, l)
f(8, l)
f(9, l)
print(l)
```

Каждый раз когда мы будем вызывать эту функцию - в каждый раз будет добавляться новые элементы

23. Что такое ленивое вычисление

происходит не в момент запуск программы а когда нам нужно получить результат какой-то функции. Мы считаем по запросу, а не все сразу. Например - генераторы, мы не считаем весь массив сразу - число Фибоначи

24. Чем отличается метод __init__ от метода __new__?

New возвращает экземпляр класса и срабатывает после new

25. Как работает сборщик мусора

В питоне есть 2 основные функции - счетчик ссылок, считает ссылки на каждый объект и если на этот объект ничего не ссылает, то он этот объект удаляет;
- Удаление циклический ссылок - это когда А ссылает на Б, и Б ссылается на А. Сборщик со временем их удаляет

26. Что означает self в классе?
Self ссылается на экземпляр класса. Так метод может обновлять объект, к которому принадлежит.

Ниже передача self в init() дает возможность установить цвет экземпляра при инициализации:

class Shirt:
    def __init__(self, color):
        self.color = color
        
s = Shirt('yellow')
s.color
#=> 'yellow'

27. Какой результат и почему

```.py
class TestException(Exception):
    pass

try:
    try:
       func()
    except TestException as e:
       raise e
       print("A")
    finally:
       print("B")
except Exception as e:
    print("C")  
finally:
    print("D")
```

28. Какой результат и почему

```.py
a=256
b=256
a is b
# True
x=257
y=257
x is y
# False
```

```.py
>>> t = (1, 2, [30, 40])
>>> t[2] += [50, 60]
```


29. Как устроены хэш таблицы?
30. Как устроен ассоциативный массив?
31. Программа использует вложенные циклы, которые могут быть недостаточно удобочитаемыми. Как вы будете его оптимизировать?
32. Python работает, как Call-by-name или Call-by-reference? (Call-by-Sharing)
33. Что такое comprehensions (list, generator, set, dictionary) в Python?
34. Вывести список уникальных элементов сохранив последовательность
a = [1, 4, 3, 2, 1, 4, 6, 7, 7]
35. Вывести словари, где значение клюза b > 150
a = [{‘a’: 140, ‘b’: 100}, {‘a’: 10, ‘b’: 150}, {‘a’: 15, ‘b’: 200}]
36. GIL, подробно, зачем надо, какие проблемы решает, как работает, какие плюсы и минусы
37. Спрашивают про асинхронность: как устроена, футуру, корутины, циклы событий, таски

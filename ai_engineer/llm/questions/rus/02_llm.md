Допы:


1. В чем разница между архитектурами Transformer: только encoder, только decoder и encoder-decoder?
2. Могут ли Transformer применяться к изображениям? / Как устроен Vision Transformer?
3. Что такое контекстное окно в LLM и почему оно имеет значение?

`Контекстное окно (context window)` — это объём рабочей памяти модели в рамках одного запроса. Оно ограничено токенами, и у разных моделей и версий может быть как больше, так и меньше. В контекстное окно могут входить: текущий вопрос пользователя, различные документы — фрагменты кода и таблицы, предыдущие диалоги, ответы модели, системный промпт и так далее.

Почему оно имеет значение:
- Если внутри контекстного окна будет много ненужной или нерелевантной информации, качество генерируемого ответа может ухудшиться.
- Стоимость токенов и скорость ответа: чем больше токенов, тем обычно дороже и медленнее ответ.
- Память и качество ответа ухудшаются, потому что при превышении лимита система может под капотом что-то обрезать или удалить — и модель просто не увидит эту информацию.

Доп.вопрос: `Почему размер контекстного окна в LLM ограничен?`

Контекстное окно ограничено потому, что увеличение длины даёт непропорциональный рост затрат на вычисления и память, требует дорогого обучения и не гарантирует, что модель реально использует всю информацию.

4. Почему первый токен в LLM обрабатывается медленнее остальных?

- В начале он делает вычисления промпта, разделяет их на токены и пытается понять как взаимосвязано все слова (токены), ну и прогоняет через все слои Transformer,затем кеширует данные (Key и value) в промпте, чтобы в дальнейшем тратить меньше времени так как все данные берутся из кеша

5. Объясните BPE (Byte Pair Encoding)
6. Что происходит после нажатие на enter в chat gpt?
7. Объясните WordPiece and SentencePiece.

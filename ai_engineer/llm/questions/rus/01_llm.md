Day_01

1. Что такое LLM и как она работает (дополнить)
  1.1. Что такое токенизация в LLM?
  1.2. Что такое эмбеддинги?
2. Что вызывает галлюцинации LLM?
3. Что такое архитектура Transformer и как она работает?
  3.1. Каковы ключевые компоненты архитектуры Transformer?
  3.2. Что такое позиционное кодирование и зачем оно нужно в Transformer?
  3.3. Что такое self-attention (самовнимание) и как оно работает?
  3.4. Почему в архитектуре Transformer мы масштабируем внимание на √dₖ?
  3.5. Что такое skip connections (residual connections) в Transformer?
  3.6. Что такое причинное маскирование (causal masking)?
  3.7. Что такое Cross Attention в Transformer?
  3.8. В чем разница между архитектурами Transformer: только encoder, только decoder и encoder-decoder?
  3.9. Чем отличаются RNN и Transformer?
  3.10. Могут ли Transformer применяться к изображениям? / Как устроен Vision Transformer?
  3.11 Какие кл.компоненты в архитектуре трансформатора
5. Объясните BPE (Byte Pair Encoding)
6. Что происходит после нажатие на enter в chat gpt?
7. Объясните WordPiece and SentencePiece.

---


1. Что такое LLM и как она работает

- LLM (Large Language Model) — это большая языковая модель, которая обучена на огромных объёмах текстовых данных. LLM может делать многое - генерировать текст, отвечать на вопросы, переводить, писать код и даже вести диалог. 

LLM работает следующим образом:
 - Он берет текст и разбивает его на токены. Токенами может быть слова, части слова или символ. Затем каждый токен получает свое id.
 - После происходит эмбединг - это когда токены превращаются в векторы (набор чисел), которые отражают их смысл и связь с другими токенами
 - Transformer с self-attention
 - Он предсказывает следующий возможный токен выбирая наиболее вероятный и затем это повторяется снова и снова. 

1.1. Что такое токенизация в LLM?

Токенизация - это когда берется текст и разбивается на различные токены. В качестве токенов может выступать слова, часть слова или символ и затем каждый токен получает свой числовой id. Стоит отметить, что нейронки работают не с текстом, а с id именно

1.2. Что такое эмбеддинги?

Эмбеддинг — это векторное представление данных. Данными могут быть слова, токены, видео или изображения. Если данные близки по смыслу или по содержанию, они находятся рядом в векторном пространстве, если нет — далеко друг от друга. 

Близость измеряется метрикой, например косинусным сходством.

2. Что вызывает галлюцинации LLM?

В первую очередь нужно понять, что LLM не проверяет правдоподобность того или иного утверждения. Он лишь предсказывает, что вероятно должно идти следующим. И если мы говорим а что вызывает галлюцинации - то например:
- У нас устаревшие данные. Условно есть какая-то библиотека и LLM обучены на 2023 году, а сейчас 2026 и это библиотека уже 100 раз обновилась, а нейронка до сих пор хранит данные с 2023 года
- Когда пользователь вводит неодназначный запрос или используют недосказанность и LLM пытается понять, что имел ввиду автор и таким образом он возвращает возможный вариант ответа и этот ответ может содержать галлюцинации

Как избавится от галлюцинаций:

Полностью от галлюцинаций мы не сможем избавиться, но мы можем например:
- написать промпт и сказать явно - если этого нет, то просто скажи - "Я не знаю";
- подключить внешнюю базу данных через rag и заставить модель отвечать на основе найденных документов
- доучить нашу модель через fine-tuning

--- 

3. Что такое архитектура Transformer и как она работает?

Transformer — это архитектура, которая обрабатывает текст целиком и параллельно. У него есть механизм self-attention, который оценивает связь и важность между токенами.

Раньше использовался подход RNN — текст читался слово за словом, но у него были проблемы с удержанием информации в памяти, особенно на длинных текстах.

---

3.1. Расскажи немного по подробней про self-attention и как оно работает?

Self-attention — это когда каждый токен сравнивается со всеми остальными через Query, Key и Value, чтобы понять, с какими токенами он связан и насколько. 

---

3.2. Что такое позиционное кодирование и зачем оно нужно в Transformer?

Positional encoding — это способ сообщить модели позицию каждого токена. Это нужно, потому что self-attention не видит порядок токенов: если у нас есть утверждение «полицейские останавливают людей» и «люди останавливают полицейских», для него это будет одно и то же. Именно эту проблему решает позиционное кодирование — чтобы модель понимала, кто идёт первым, а кто вторым.

---

3.4. Что такое Multi-Head Attention (MHA)

Multi-Head Attention — это когда несколько self-attention работают параллельно, и каждый из них следит за своей зоной ответственности: например, один — за синтаксисом, второй — за пунктуацией, третий — за позицией, четвёртый — за поиском синонимов. А потом все головы склеиваются вместе.

3.5. Что такое Multi-Query Attention (MQA)

...

3.5. Что такое Grouped-Query Attention (GQA) и чем он отличается от Multi-Head Attention (MHA)?

...

---

3.6. Что такое Feed-Forward Networks в LLM?

---

3.7. Что такое причинное маскирование (causal masking)?

---

3.8. Что такое Cross Attention в Transformer?

---

3.9. Каковы ключевые компоненты архитектуры Transformer?

---


3.4. Почему в архитектуре Transformer мы масштабируем внимание на √dₖ?

=> 3.5. Что такое skip connections (residual connections) в Transformer?

3.8. В чем разница между архитектурами Transformer: только encoder, только decoder и encoder-decoder?

3.10. Могут ли Transformer применяться к изображениям? / Как устроен Vision Transformer?

// ! Array.prototype.concat() - создает новый массив, соединяя два предыдущих массива

const array1 = ['a', 'b', 'c'];
const array2 = ['d', 'e', 'f'];
array1.concat(array2);              // ? [a, b, c, d, e, f]
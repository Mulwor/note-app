## В каком порядке?

```js
document.querySelector("body").addEventListener("click", () => console.log(1));

setTimeout(() => console.log(2), 0);

for (var i = 0; i < 1000000000000; i++) {
  /*code*/
}

console.log(3);
```

Задача на базовое понимание флоу программ на JS. Разминка перед написанием кода.

#### Ответ сеньора:

Кандидат отвечает как сработает код, попутно рассказывает про устройство event-loop. Какие очереди работают? Содержимое очередей после каждой строчки кода? Что будет, если в разное время пытаться нажать на страницу?

#### Ответ мидла:

У кандидата есть опыт, он уже видел подобный код, и по наитию может дать ответ. Затрудняется дать ответ про устройство event-loop.

## Compose Function

```js
// example 1
const add2 = (x) => x + 2;
const composition = compose(add2, Math.sqrt); // Math.sqrt(add2(x))

composition(2); // => 2

// example 2
const dec = (x) => x - 1;
const pow2 = (x) => x ** 2;

compose(dec, pow2)(2); // => 1
compose(pow2, dec)(2); // => 3

// solution

function compose() {}
```

Задача размять руки перед задачей со звездочкой.

#### Решение:

Еще раз уточняет требования, пишет общий случай, пропуская частный на два агрумента. Использует ли гугл? Сколько подсказок использует для решения. Может ли объяснить свое решение. Код субъективен, поэтому оцениваем читабельность, скорость решения, кол-во подсказок. Задание на 10 мин максимум. Кол-во подсказок для синя 0 потому что есть тесты.

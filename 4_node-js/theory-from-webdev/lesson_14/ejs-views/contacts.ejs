<!DOCTYPE html>
<html lang="en">
<%- include('./partial/head.ejs') %>
<body>
  <%- include('./partial/nav.ejs') %>
  <h1>Contacts:</h1>
  <ul>
    <% if (contacts.length) { %>
    <% contacts.forEach(({link, name}) => { %>
      <li><a href="%= link %"><%= name %></a></li>
    <% }) %>
    <% } %>
  </ul>
</body>
</html>
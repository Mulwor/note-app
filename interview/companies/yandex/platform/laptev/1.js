// Необходимо написать функцию strjoin, котора склеивает строки через разделитель

function strjoin() {
  // code here
}

console.log(strjoin('.', 'a', 'b', 'c')); // 'a.b.c.
console.log(strjoin('-', 'a', 'b', 'c', 'd', 'e', 'f')); // a-b-c-d-e-f

// 1.1. Доп.вопрос, что вернет если мы добавим только разделитель
console.log(strjoin('.'));

// 1.2. Напиши решение с помощью ЕС6, и до ЕС5;

// 1.3. Что такое arguments
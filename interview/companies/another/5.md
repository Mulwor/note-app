Задача №1 - напишите программу, которая по списку детей и их игрушек определяет список игрушек, которые есть только у одного ребёнка. Выведите их в алфавитном порядке.

```js
const anya = 'Аня: кукла, машинка, кукла, домик';
const borya = 'Боря: машинка, зайчик';
const vova = 'Вова: кубики, машинка';

function findUniqueToys(...kids) {
  const toysCount = {};

  // собираем все игрушки
  for (const kid of kids) {
    const [, toysString] = kid.split(':');
    const toys = toysString.split(',').map(t => t.trim());

    for (const toy of toys) {
      toysCount[toy] = (toysCount[toy] || 0) + 1;
    }
  }

  // фильтруем уникальные (которые встречаются 1 раз)
  const uniqueToys = Object.keys(toysCount)
    .filter(toy => toysCount[toy] === 1)
    .sort((a, b) => a.localeCompare(b));

  // выводим
  for (const toy of uniqueToys) {
    console.log(toy);
  }
}

findUniqueToys(anya, borya, vova);

// домик
// зайчик
// кубики
// кукла
```

Задача №2 - что будет в консоль логе

```js
let firstObj = { greeting: 'hey' };
let secondObj = firstObj;

firstObj.greeting = 'ho';

console.log(secondObj.greeting); // ?
firstObj = { greeting: 'hello' };

console.log(secondObj.greeting); // ?
```

Задача №3 - отсортировать массив

```js
const input = [
  { order: 4, value: 'abcd' },
  { order: 2, value: 'qwer' },
  { order: 1, value: 'xyz1' },
  { order: 3, value: 'abx2' },
];

// обычная запись:
// input.sort((a, b) => a.order - b.order);

// деструктурирующее присваивание в параметрах функции:
input.sort(({ order: orderA }, { order: orderB }) => orderA - orderB);

console.log(input);
```

Задача №4 - вывод порядок консоль лога

```js
setTimeout(() => {
  console.log('timeOut');
}, 0);

console.log(1);

new Promise((resolve) => {
  console.log('Promise');
  setTimeout(() => {
    console.log('777');
    resolve();
  });
})
  .then(() => {
    console.log('then1');
  })
  .then(() => {
    console.log('then2');
  });

console.log(4);

setTimeout(() => {
  console.log('timeOut2');
}, 0);
```

Задача №5 - протипизировать

```js
import React from "react";

export default () => <div>Hello</div>;

// Пример объектов
const X = { a: 1, b: 2, c: 3, d: 4 };
const Y = { l: 1, k: 2, m: true, n: 4 };

// Типизированная функция
function getProperty<
  T extends object, // тип объекта
  K extends keyof T // ключ должен быть одним из ключей объекта
>(obj: T, key: K): T[K] {
  return obj[key];
}

// ✅ корректные вызовы:
console.log(getProperty(X, 'a')); // 1
console.log(getProperty(X, 'd')); // 4
console.log(getProperty(Y, 'm')); // true

// 🚫 некорректные вызовы (ошибка типов):
// getProperty(X, 'z'); // ❌ Error: Argument of type '"z"' is not assignable to parameter of type '"a" | "b" | "c" | "d"'
```

Задача №6 - что выводится после чего и почему

```js
import { useState, useEffect, useLayoutEffect } from "react";

const App = () => {
  const [num, setNum] = useState(0);

  function clickHandler() {
    setNum((prev) => prev + 1);
  }

  console.log('App render');

  useLayoutEffect(() => {
    console.log('useLayoutEffect');
    return () => {
      console.log('cleanup useLayoutEffect');
    };
  }, [num]);

  useEffect(() => {
    console.log('useEffect');
    return () => {
      console.log('cleanup useEffect');
    };
  }, [num]);

  return (
    <>
      <button onClick={clickHandler}>render</button>
      <div style={{ fontSize: "45px", textAlign: "center" }}>{num}</div>
    </>
  );
};

export default App;
```
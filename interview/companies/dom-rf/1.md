1\. Какие знаешь архитектуры: чем они хороши и для каких ситуаций?

✅ 2\. Что выведет: `console.log([] === []);`

✅ 3\. Что произойдёт?

```js
let str = 'somestr'
console.log(str[1])
str[1] = 'h'
console.log(str[1])
```

<details>
<summary>Ответ</summary>

Необходимо начать с того, что у нас строки иммутабельны. Таким образом на строке 9 у нас вызовется o, потом мы якобы пытаемся изменить str[1] = h и если строгий режим включен он вызовет ошибку, а если нет, то ничего и опять вернет нам h
</details>

✅ 4\. Расскажи,что делает каждое выражение

```js
a => ?.
b => ...
c => ??
d => &&=
e => `${foo}`
f => structuredClone(bar)
g => someArray.at()
```

<details>
<summary>Ответ</summary>

a. Обращение к свойству объекта, если не найдет то null/undefined
b. Спред оператор - когда нам нужно объединить два массива или в объекта в один общий
c. Оператор нулевого слияние. Если слева у нас null/undefined, то возвращает правый операнд
d. Логическое присваивание И - присваивает значение если оно истинное
e. Шаблонный литерал - когда берем какое-то значение и добавляем внутрь строки (шаблонного литерала)
f. Создает глубокое копирования для примитов, объектов, даты мапов, хешов и прочее
g. Один из способов вернуть элемент массива по индексу. Ну и например если мы хотим последний индекс использовать можем просто -1 написать, а раньше arr.length - 1

</details>

✅ 5\. Напиши функцию прототип для String capitalize ~ `'asd'.capitalize() => Asd`

```js
String.prototype.capitalize = function () {
  return this[0].toUpperCase() + this.slice(1).toLowerCase();
}
```

6\. Чем отличаются Object от Map / Set / Array?

7\. Когда нужен Map, а когда Object достаточно?

8\. Когда нужен Set, а когда Object достаточно? (повтор вопроса)

9\. Задача: вернуть новый массив, состоящий из уникальных (встречаются ровно 1 раз) элементов исходного.

```js
const test = [10, 5, 10, 0, 6, 6, 7, 2, 9, 9]
```


✅ 10\. Расскажи про event loop и что выведется на консоль?

```js
setTimeout(() => {
  console.log("timeOut");
}, 0);

console.log(1);

new Promise(resolve => {
  console.log("Promise");
  setTimeout(() => {
    console.log("777");
    resolve();
  }, 0);
}).then(() => {
    console.log("then1");
}).then(() => {
    console.log("then2");
});

console.log(4);

setTimeout(() => {
  console.log("timeOut2");
}, 0);
```

Ответ: 1, Promise, 4, timeOut, 777, then1, then2, timeOut2

✅ 11\. Расскажи про статические методы Promise

✅ 12\. Расскажи про async/await

✅ 13\. Нужно вывести строку в обратном порядке без дублей, объекты отсортированы по order и должны быть не expired

```js
const input = [
  { value: 'abcd', order: 3, expired: false },
  { value: 'qwer', order: 2, expired: true },
  { value: 'xyz0', order: 1, expired: false },
  { value: 'abx1', order: 3, expired: false },
];

const sortInput = (array) => {
  return array.sort((a, b) => b.order - a.order).filter((element) => !element.expired)
}
```

✅ 14\. Чем отличаются useEffect / useLayoutEffect?

<details>
<summary>Ответ</summary>

useLayoutEffect синхронный и вызывается до отрисовки, а useEffect ассихнронный и срабатывает после того как браузер отрисовать страницу
</details>

✅ 15\. Сколько раз и когда вызываются:

```js
useEffect(() => { ... })
useEffect(() => { ... }, [])
useEffect(() => { ... }, [a, b])
```

<details>
<summary>Ответ</summary>

В первом случае вызывается каждый раз при рендере страницы
Во втором случае вызывается только при монтировании страницы
И в третьем случае когда у нас обновляются переменные a и b
</details>

16\. Для чего нужен useRef?

17\. Как оптимизировать:

```js
const heavyFunc = (count) => {
  // hard calculations
};

const LazyInit = (props) => {
  const [count, setCount] = useState(heavyFunc(props.count));
  ...
};
```

✅ 18\. Как оптимизировать:

```js
const increment = useCallback(() => {
  setCount(prev + 1);
}, []);
```

<details>
<summary>Ответ</summary>

`setCount((prev) => prev + 1);`
</details>


✅ 19\. Как сделать, чтобы тут была не 1:

```js
setTimeout(() => {
  setClicks(clicks + 1);
}, 2000);
```

<details>
<summary>Ответ</summary>

`setClicks((clicks) => clicks + 1);`
</details>


✅ 19\. Чем отличаются `any / unknown / never / void`?

✅ 20\. Напиши код, чтобы была ошибка на: `getProperty(x, "m");`

<details>
<summary>Решение</summary>

```js
function getProperty<T, K extends keyof T>(obj: T, key: K): T[K] {
  return obj[key]
} 

getProperty(X, "a");
getProperty(X, 'm');

// Объяснения
1. Есть параметры типа T, это тип объекта, которую мы будем передавать в эту функцию в данном случае это X;
2. Второй параметр K - здесь мы используем ограничения и оно говорит, что K у нас должен быть одним из ключей объекта Т. Если мы передаем 'a', то проверка сработает, так как ключ у нас есть, а если нет, то выдаст ошибку, а вот ключа 'm' у нас нет по этому и выведится такая ошибка
```
</details>
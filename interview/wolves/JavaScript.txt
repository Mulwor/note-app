Блокирует ли await текущую функцию или весь поток?

В чём опасность innerHTML с точки зрения безопасности?

В чём разница между == и ===?

В чём разница между Function Declaration и Function Expression?

В чём разница между spread и rest?

В чём польза замыканий на практике?

В чём ошибка с var в замыканиях и цикле?

Выполнится ли finally, если в try есть return?

Гарантирует ли setTimeout выполнение ровно через N мс?

Есть ли arguments в стрелочных функциях?

Есть ли многопоточность в JS? Как достичь параллелизма?

Зачем вызывать setTimeout(fn, 0)?

Зачем используют spread при вызове функции?

Зачем используют двойное отрицание !!?

Зачем соблюдать иммутабельность в функциях?

Зачем нужен break в switch? Что будет без него?

Зачем переопределять toString() у объекта?

Из чего состоит контекст выполнения?

Как в ES6 объявляют классы?

JavaScript – какие виды циклов есть?

Как args передаются в callback?

Как работает "микротаска промиса"?

Как работает await?

Как работает event loop?

Как работает hoisting для функций?

Как работает логический оператор && (короткое замыкание)?

Как работает MutationObserver?

Как работает постфиксный/префиксный инкремент/декремент?

Как работает sort()? В чём подвох с числами?

Как работает spread для массива?

Как работает тернарный оператор ? :?

Как работает tree shaking при сборке?

Как работает цепочка промисов?

Как работает цикл for...in? Чем отличается от for...of?

Как работает цикл for...of?

Как работает WebSocket?

Как добавить метод в класс?

Как добавить метод в объект после создания?

Как добавить новое свойство в объект?

Как задать параметры по умолчанию?

Как задать значения по умолчанию для свойств класса?

Как задать значения по умолчанию при деструктуризации?

Как заблокировать прокрутку страницы?

Как загрузить скрипт с async/defer – что делает атрибут async у script?

Как загрузить скрипт с async/defer – что делает атрибут defer?

Как замерить производительность JS-кода?

Как избежать callback hell?

Как избежать утечек памяти?

Как из arguments получить настоящий массив?

Как использовать async/await для асинхронных запросов?

Как копировать свойства через Object.assign?

Как минимизировать количество HTTP-запросов?

Как объединять строки в JavaScript?

Как объявить приватное свойство в классе?

Как обрабатывать ошибки в JavaScript?

Как обрабатывать ошибки внутри функций?

Как обрабатывать ошибки в async-функциях?

Как обойти двумерный массив?

Как отменить выполнение промиса?

Как парсить и сериализовать JSON в JavaScript?

Как передать callback в другую функцию?

Как передать параметры в callback?

Как поверхностно скопировать массив?

Как получить последний элемент массива?

Как правильно отсортировать массив чисел?

Как правильно проверить, что значение — массив?

Как при деструктуризации пропустить элементы?

Как примитивы и объекты хранятся в памяти?

Как проверить наличие свойства в объекте?

Как проверить значение на NaN?

Как профилировать использование памяти?

Как работает requestAnimationFrame?

Как работает requestIdleCallback?

Как работает Service Worker?

Как работает SharedWorker?

Как работают геттеры и сеттеры в классе?

Как работают геттеры и сеттеры в объекте?

Как работают import и export?

Как работают потоки (ReadableStream, WritableStream)?

Как работает JSON.stringify?

Как работает structuredClone?

Как работает временная мёртвая зона (TDZ)?

Как разбить тяжёлую задачу, чтобы не блокировать UI?

Как реализовать мемоизацию функции?

Как реализовать функцию compose?

Как создать объект без прототипа?

Как создать собственный тип ошибки?

Как создать элемент в DOM?

Как создать ошибку вручную?

Как таймеры и слушатели событий влияют на память?

Как удалить свойство из объекта?

Как удалить элемент из DOM?

Как увеличить производительность – что даёт минификация кода?

Какие данные в JS неизменяемые, какие изменяемые?

Какие методы массива: every, some, find, filter, map, reduce, forEach?

Какие метрики используют для оценки производительности?

Какие основные методы для работы с массивом вы знаете?

Какие селекторы использовать для поиска элементов в DOM?

Какие способы объявления объекта вы знаете?

Какие способы объявления переменной есть в JS?

Какие способы объявления функций есть в JS?

Какие значения в JS считаются truthy и falsy?

Какие типы асинхронных операций вы знаете?

Какие типы данных есть в JavaScript?

Какие типичные утечки памяти в SPA?

Какие условные конструкции есть в JS?

Когда лучше не использовать стрелочные функции?

Когда методы массива эффективнее циклов и наоборот?

Когда рекурсию лучше заменить циклом?

Когда используют скобочную нотацию для свойств?

Могут ли в одном массиве быть элементы разных типов?

Можно ли использовать await в стрелочной функции?

Можно ли иметь несколько конструкторов в классе?

Можно ли наследовать приватные свойства?

Можно ли не определять конструктор в классе?

Можно ли обратиться к приватному свойству вне класса?

Передаются ли аргументы по значению или по ссылке?

Передаются ли примитивы и объекты по ссылке или по значению?

Почему JS считается однопоточным?

Почему Web Worker не имеет доступа к DOM?

Почему [] == false в JavaScript? Как работает сравнение?

Почему ES6 считают самым важным обновлением?

Почему нельзя копировать объект оператором =?

Почему не стоит блокировать event loop?

Почему цикл с var и setTimeout выводит одно и то же значение?

Почему typeof null возвращает "object"?

Что возвращает async-функция?

Что возвращает функция без return?

Что возвращает генератор?

Что возвращает Object.entries()?

Что возвращает Object.keys()?

Что возвращает Object.values()?

Что возвращает toString() у функции?

Что возвращает typeof [] и почему?

Что возвращается при обращении к отсутствующему свойству?

Что делает AbortController и AbortSignal?

Что делает addEventListener() и removeEventListener()?

Что делает Array.from()?

Что делает Array.of()?

Что делает await?

Что делает catch() в промисах?

Что делает debounce и когда его применять?

Что делает delete arr[0]? Остаётся ли «дыра» в массиве?

Что делает document.cookie?

Что делает event.preventDefault()?

Что делает event.stopPropagation()?

Что делает event.target?

Что делает finally в try-catch?

Что делает flatMap()?

Что делает form.reset()?

Что делает Function.prototype.apply()?

Что делает Function.prototype.bind()?

Что делает Function.prototype.call()?

Что делает innerHTML?

Что делает instanceof Error?

Что делает IntersectionObserver?

Что делает join()?

Что делает localStorage?

Что делает map() у массива?

Что делает navigator?

Что делает new?

Что делает Object.create()?

Что делает Object.defineProperty()?

Что делает Object.freeze()?

Что делает Object.preventExtensions()?

Что делает Object.values()?

Что делает pop()?

Что делает postMessage() между потоками?

Что делает Promise.all()?

Что делает Promise.any()?

Что делает Promise.race()?

Что делает Promise.withResolvers()?

Что делает push()?

Что делает queueMicrotask()?

Что делает reduce()?

Что делает Reflect.defineProperty()?

Что делает removeEventListener?

Что делает return внутри конструктора?

Что делает reverse()? Мутирует ли массив?

Что делает sessionStorage?

Что делает setInterval()?

Что делает setTimeout()?

Что делает shift()?

Что делает shift()? (уточнение для массива)

Что делает some()?

Что делает splice()? Чем отличается от slice()?

Что делает split()? (метод строк)

Что делает structuredClone?

Что делает throttle и когда его применять?

Что делает toJSON()?

Что делает toReversed()?

Что делает toSorted()?

Что делает toSpliced()?

Что делает unshift()?

Что делает void?

Что делает window.location?

Что делает window?

Что значит запись const A = class {}?

Что значит «функция без состояния»?

Что значит “неблокирующий код”?

Что измеряют Core Web Vitals?

Что нового принёс ES6 для асинхронности?

Что происходит, когда в коде возникает ошибка?

Что произойдёт, если в catch возникнет новая ошибка?

Что произойдёт, если изменить length у массива вручную?

Что произойдёт, если не поставить await?

Что произойдёт при обращении к переменной до её объявления?

Что такое “bottleneck” в алгоритмах?

Что такое “code splitting”?

Что такое “macrotask queue”?

Что такое “microtask queue”?

Что такое анонимная функция?

Что такое асинхронность в JavaScript?

Что такое асинхронный генератор?

Что такое базовый случай в рекурсии?

Что такое блокирующая операция?

Что такое всплытие (hoisting)?

Что такое генератор в JavaScript?

Что такое глобальный объект в JS?

Что такое глубокая рекурсия и какие риски?

Что такое динамическая типизация?

Что такое динамический import() и когда его использовать?

Что такое деструктуризация?

Что такое замыкание (closure)?

Что такое исключение (exception)?

Что такое индекс массива? С какого значения он начинается?

Что такое каррирование?

Что такое класс в JavaScript?

Что такое композиция функций?

Что такое конструктор класса?

Что такое контекст выполнения функции (this)?

Что такое лексическая область видимости?

Что такое ловушки (traps) у Proxy?

Что такое массив в JavaScript?

Что такое мемоизация и зачем она нужна?

Что такое микротаски (microtasks)?

Что такое область видимости (scope)?

Что такое объект в JavaScript?

Что такое объект arguments?

Что такое объект Proxy?

Что такое объектный литерал?

Что такое отладка?

Что такое очередь задач (task queue)?

Что такое пара ключ-значение в объекте?

Что такое псевдомассив? Как отличить от массива?

Что такое приведение типов (type coercion)?

Что такое приватное свойство (классы)?

Что такое прототип объекта?

Что такое рекурсия?

Что такое сокращённая запись свойств ({ x, y })?

Что такое стек вызовов (call stack)?

Что такое стрелочные функции?

Что такое утечка памяти в JS?

Что такое функция высшего порядка?

Что такое функция-декоратор?

Что такое функция-обёртка?

Что такое функция-фабрика?

Что такое хвостовая рекурсия?

Что такое цепочка прототипов?

Что такое чистая функция?

Что такое шаблонные литералы (template literals)?

Что такое event loop?

Что такое execution context (контекст выполнения)?

Что такое Function Constructor?

Что такое IIFE и зачем он нужен?

Что такое instanceof?

Что такое JSON?

Что такое Map? Когда использовать вместо объекта?

Что такое NaN?

Что такое Promise?

Что такое scope chain?

Что такое Set? Когда его использовать?

Что такое Shadow DOM?

Что такое Symbol и зачем он нужен?

Что такое tagged template literal?

Что такое tree shaking?

Что такое WeakMap? Чем отличается от Map?

Что такое WeakRef?

Что такое WeakSet? Чем отличается от Set?

Что хранится в proto?

Чем proto отличается от prototype?

Чем атрибуты отличаются от свойств у DOM-элемента?

Чем BigInt отличается от обычного числа?

Чем класс отличается от функции-конструктора?

Чем DOM отличается от HTML?

Чем DOMContentLoaded отличается от load?

Чем event.target отличается от event.currentTarget?

Чем Reflect отличается от Object?